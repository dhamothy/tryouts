.{$pre_global}-{$nav}{

	&__{
		&list{
			display: flex;
		}
		&item{
			padding: 6px 9px;
		    text-transform: uppercase;
		    font-size: 10px;
		    letter-spacing: 1.2px;
		    color: currentColor;

		    &:hover{
		    	color: $color_primary;
		    }
		}
	}
}